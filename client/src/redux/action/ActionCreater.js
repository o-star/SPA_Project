import { HOME, INTRODUCTION, ESTIMATION, STATISTICS } from './ActionType'

export const home = () => ({ type: HOME })
export const introduction = () => ({ type: INTRODUCTION })
export const estimation = () => ({ type: ESTIMATION })
export const statistics = () => ({ type: STATISTICS })